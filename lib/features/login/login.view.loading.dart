import 'package:flutter/material.dart';

class LoginViewLoading extends StatelessWidget {
  const LoginViewLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('LOADING'));
  }
}
