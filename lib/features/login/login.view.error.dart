import 'package:flutter/material.dart';

class LoginViewError extends StatelessWidget {
  const LoginViewError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ERROR'));
  }
}
